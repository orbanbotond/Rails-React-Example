<%= form_for(@ingredient_suggestion) do |f| %>
  <% if @ingredient_suggestion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ingredient_suggestion.errors.count, "error") %> prohibited this ingredient_suggestion from being saved:</h2>

      <ul>
      <% @ingredient_suggestion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item %><br>
    <%= f.text_field :item %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
